###############################################################################
# Runtime dependencies for uni2path
#
# Policy:
# - Use compatible version ranges (>=,<) to avoid non-existent or future pins.
# - Keep the set minimal for current features (UC1: pathways + Streamlit UI).
# - Pydantic v2 is required (FastAPI 0.110+). No v1 support.
###############################################################################

# API framework (Pydantic v2 series)
fastapi>=0.110,<1.0
pydantic>=2.4,<3.0

# ASGI server (for local run)
uvicorn[standard]>=0.23,<1.0

# HTTP client + retry helper
httpx>=0.25,<1.0
backoff>=2.2,<3.0

# UI
streamlit>=1.25,<2.0

# Future (M2/M3) - network analysis and visualization
networkx>=3.1,<4.0
pyvis>=0.3,<1.0

# Optional: if PNG/SVG export or dataframes are later required, add:
# pandas>=2.0,<3.0
# plotly>=5.0,<6.0

