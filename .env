# uni2path environment example

# FastAPI base URL used by Streamlit UI
UNI2PATH_API=http://localhost:8000

# Log level for JSON logger: DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO

# Local data directory (SQLite cache, snapshots, etc.)
UNI2PATH_DATA=.data

# Corporate proxy (optional)
#HTTP_PROXY=http://proxy.example.com:8080
#HTTPS_PROXY=http://proxy.example.com:8080
#NO_PROXY=localhost,127.0.0.1

# Custom CA bundle path if your network uses an internal CA (optional)
#REQUESTS_CA_BUNDLE=C:\\path\\to\\corp-ca.pem

